version: 1

definitions:
  defaults:
    # default repository. set to git remote repository before deployment
    - &defaultRepo "https://github.com/kptdobe/helix-sections-playground.git#master"

  strains:
    base:
      &basestrain
      code: *defaultRepo
      content: *defaultRepo
      static: *defaultRepo

strains:
  - name: default
    <<: *basestrain
    package: acapt/d9a66d9790c4077ed1a39b133d704667e582fe09
