version: 1

definitions:
  defaults:
    # default repository. set to git remote repository before deployment
    - &defaultRepo "https://github.com/kptdobe/helix-sections-playground.git#master"

  strains:
    base:
      &basestrain
      code: *defaultRepo
      content: *defaultRepo
      static: *defaultRepo

strains:
  - name: default
    <<: *basestrain
    package: acapt/f1876eb358d28b9e031fe696ac616735dc863795
